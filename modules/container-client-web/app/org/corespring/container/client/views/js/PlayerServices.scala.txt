@(namespace : String,
psEndpoints: org.corespring.container.client.views.models.PlayerServicesEndpoints,
queryParams : play.api.libs.json.JsValue)

angular.module('@namespace', []);

angular.module('@namespace').factory('PlayerService', ['PlayerServiceDefinition',
  function(PlayerServiceDefinition) {
    var params = @{Html(play.api.libs.json.Json.stringify(queryParams))};
    var urls = @psEndpoints.toJsonString;
    return new PlayerServiceDefinition(urls, params);
  }
]);
