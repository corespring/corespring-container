package org.corespring.container.client.io

import org.specs2.mutable.Specification

class ResourcePathTest extends Specification {

  "lastModified" should {
    "return None if an error is thrown" in pending
    "return Some(long) from the url connection" in pending
  }

  "loadPath" should {
    "return None if an error is thrown" in pending
    "return Some(string) from the url" in pending
  }
}
